const screenInfo = {
    getScreenWidth() {
        var w = window.screen.width;
        return w;
    },
    getScreenHeight() {
        var h = window.screen.height;
        return h;
    }
};

export { screenInfo };
